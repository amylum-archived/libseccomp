name 'libseccomp'
org 'amylum'

deps(
  'linux-api-headers': '4.11-2'
)

configure_flags(
  prefix: '/usr'
)

cflags
harden

build do
  run './autogen.sh'
  configure
  make
  install
end

test do
  # TODO: add tests
end
